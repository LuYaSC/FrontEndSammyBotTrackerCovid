import { OnlyIntegerDirective } from './only-integer.directive';

describe('OnlyIntegerDirective', () => {
  it('should create an instance', () => {
    // const directive = new OnlyIntegerDirective();
    // expect(directive).toBeTruthy();
  });
});

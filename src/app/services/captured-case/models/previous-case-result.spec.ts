import { PreviousCaseResult } from './previous-case-result';

describe('PreviousCaseResult', () => {
  it('should create an instance', () => {
    expect(new PreviousCaseResult()).toBeTruthy();
  });
});
